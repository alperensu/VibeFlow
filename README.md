# VibeFlow
VibeFlow is a high-performance, editor-agnostic context optimizer that slashes LLM token usage by up to 70%. It acts as an intelligent middleware that prunes unnecessary code, extracts minimal dependencies using AST, and ensures your AI coding assistant only sees what it needs to see.
